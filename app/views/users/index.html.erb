<%= provide(:title, 'ユーザー一覧') %>
<h1>Users</h1>
<div class ="row">
  <div class ="col-md-3">
    <%= render 'shared/search' %>
  </div>
  <div class ="col-md-9 ">
    <ul class ="users">
      <%= paginate @users %>
      <%= render @users %>
      <%= paginate @users %>
    </ul>
  </div>
</div>
