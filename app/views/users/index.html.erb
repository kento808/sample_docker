<%= provide(:title, 'ユーザー一覧') %>
<h1>Users</h1>
<div class ="row">
  <div class ="col-md-8 col-md-offset-2">
    <ul class ="users">
      <%= paginate @users %>
      <%= render @users %>
      <%= paginate @users %>
    </ul>
  </div>
</div>
