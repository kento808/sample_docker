<%= provide(:title, 'ユーザー一覧') %>
<h1>Users</h1>
<div class ="row">
  <div class ="col-md-8 mx-auto">
    <%= paginate @users %>
    <ul class ="users">
      <%= render @users %>
    </ul>
    <%= paginate @users %>
  </div>
</div>
