<h2>お気に入りの投稿 ( <%= @posts.count %> )</h2>
<div class ="row">
  <div class ="col-md-6 col-md-offset-3">
    <% if @posts.any? %>
      <%= paginate @posts %>
      <ol class ="microposts">
        <%= render @posts %>
      </ol>
      <%= paginate @posts %>
    <% else %>
      <h3>お気に入りはまだありません</h3>
    <% end %>
  </div>
</div>
